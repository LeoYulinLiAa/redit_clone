<h1>Subs</h1>
<% if logged_in? %>
  <a class="button primary-button" href="<%= new_sub_path %>">Create new Sub</a>
<% end %>
<ul>
  <% @subs.each do |sub| %>
    <li><a href="<%= sub_path(sub) %>"><%= sub.title %></a></li>
  <% end %>
</ul>